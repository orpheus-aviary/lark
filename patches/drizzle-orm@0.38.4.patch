diff --git a/expo-sqlite/session.cjs b/expo-sqlite/session.cjs
index 1556d939bfbdf0001d7ae5680ad32bccb40a90b2..a13840a9012a8a3565840b55bcc1f6a7fb6b5edc 100644
--- a/expo-sqlite/session.cjs
+++ b/expo-sqlite/session.cjs
@@ -29,6 +29,47 @@ var import_sql = require("../sql/sql.cjs");
 var import_sqlite_core = require("../sqlite-core/index.cjs");
 var import_session = require("../sqlite-core/session.cjs");
 var import_utils = require("../utils.cjs");
+// ─── lark patch (Phase B N0b-2, criterion 17b) ──────────
+//
+// Upstream `prepareQuery()` calls `client.prepareSync()` and hands the native
+// statement to the prepared query, which never finalizes it. Measured on a real
+// device: 10,000 selects prepare 10,000 statements and release none
+// (drizzle-orm#4519 — open, no maintainer response, no linked PR).
+//
+// The fix keeps the CLIENT and the SQL instead of the statement, and prepares
+// inside each method. Two constraints shape it:
+//
+//   1. Expo's `executeSync()` returns a cursor that still has to be read, so
+//      the result is consumed BEFORE the statement is released. Releasing first
+//      passes every write and breaks every query.
+//   2. After a FAILED execute, `finalizeSync()` throws — and what it throws is
+//      the statement's own last error ("... has been rejected. -> Caused by:
+//      Error code : UNIQUE constraint failed: ..."). That is
+//      `sqlite3_finalize()` as documented: it reports the last evaluation's
+//      code and destroys the statement either way. Letting it out of the
+//      `finally` would replace the real error with a message about
+//      finalizeSync, so it is swallowed — but ONLY when an error is already on
+//      its way up.
+function larkWithStatement(client, sqlText, execute, consume) {
+  const stmt = client.prepareSync(sqlText);
+  let failed = false;
+  try {
+    return consume(execute(stmt));
+  } catch (err) {
+    failed = true;
+    throw err;
+  } finally {
+    if (failed) {
+      try {
+        stmt.finalizeSync();
+      } catch {
+        // Already reported by the error being propagated.
+      }
+    } else {
+      stmt.finalizeSync();
+    }
+  }
+}
 class ExpoSQLiteSession extends import_session.SQLiteSession {
   constructor(client, dialect, schema, options = {}) {
     super(dialect);
@@ -39,9 +80,8 @@ class ExpoSQLiteSession extends import_session.SQLiteSession {
   static [import_entity.entityKind] = "ExpoSQLiteSession";
   logger;
   prepareQuery(query, fields, executeMethod, isResponseInArrayMode, customResultMapper) {
-    const stmt = this.client.prepareSync(query.sql);
     return new ExpoSQLitePreparedQuery(
-      stmt,
+      this.client,
       query,
       this.logger,
       fields,
@@ -80,9 +120,9 @@ class ExpoSQLiteTransaction extends import_sqlite_core.SQLiteTransaction {
   }
 }
 class ExpoSQLitePreparedQuery extends import_session.SQLitePreparedQuery {
-  constructor(stmt, query, logger, fields, executeMethod, _isResponseInArrayMode, customResultMapper) {
+  constructor(client, query, logger, fields, executeMethod, _isResponseInArrayMode, customResultMapper) {
     super("sync", executeMethod, query);
-    this.stmt = stmt;
+    this.client = client;
     this.logger = logger;
     this.fields = fields;
     this._isResponseInArrayMode = _isResponseInArrayMode;
@@ -92,18 +132,24 @@ class ExpoSQLitePreparedQuery extends import_session.SQLitePreparedQuery {
   run(placeholderValues) {
     const params = (0, import_sql.fillPlaceholders)(this.query.params, placeholderValues ?? {});
     this.logger.logQuery(this.query.sql, params);
-    const { changes, lastInsertRowId } = this.stmt.executeSync(params);
-    return {
-      changes,
-      lastInsertRowId
-    };
+    return larkWithStatement(
+      this.client,
+      this.query.sql,
+      (stmt) => stmt.executeSync(params),
+      (result) => ({ changes: result.changes, lastInsertRowId: result.lastInsertRowId })
+    );
   }
   all(placeholderValues) {
-    const { fields, joinsNotNullableMap, query, logger, stmt, customResultMapper } = this;
+    const { fields, joinsNotNullableMap, query, logger, client, customResultMapper } = this;
     if (!fields && !customResultMapper) {
       const params = (0, import_sql.fillPlaceholders)(query.params, placeholderValues ?? {});
       logger.logQuery(query.sql, params);
-      return stmt.executeSync(params).getAllSync();
+      return larkWithStatement(
+        client,
+        query.sql,
+        (stmt) => stmt.executeSync(params),
+        (result) => result.getAllSync()
+      );
     }
     const rows = this.values(placeholderValues);
     if (customResultMapper) {
@@ -114,9 +160,14 @@ class ExpoSQLitePreparedQuery extends import_session.SQLitePreparedQuery {
   get(placeholderValues) {
     const params = (0, import_sql.fillPlaceholders)(this.query.params, placeholderValues ?? {});
     this.logger.logQuery(this.query.sql, params);
-    const { fields, stmt, joinsNotNullableMap, customResultMapper } = this;
+    const { fields, client, joinsNotNullableMap, customResultMapper } = this;
     if (!fields && !customResultMapper) {
-      return stmt.executeSync(params).getFirstSync();
+      return larkWithStatement(
+        client,
+        this.query.sql,
+        (stmt) => stmt.executeSync(params),
+        (result) => result.getFirstSync()
+      );
     }
     const rows = this.values(placeholderValues);
     const row = rows[0];
@@ -131,7 +182,12 @@ class ExpoSQLitePreparedQuery extends import_session.SQLitePreparedQuery {
   values(placeholderValues) {
     const params = (0, import_sql.fillPlaceholders)(this.query.params, placeholderValues ?? {});
     this.logger.logQuery(this.query.sql, params);
-    return this.stmt.executeForRawResultSync(params).getAllSync();
+    return larkWithStatement(
+      this.client,
+      this.query.sql,
+      (stmt) => stmt.executeForRawResultSync(params),
+      (result) => result.getAllSync()
+    );
   }
   /** @internal */
   isResponseInArrayMode() {
diff --git a/expo-sqlite/session.d.cts b/expo-sqlite/session.d.cts
index 800145df4d886254c7978e7bf5db42f635fa140d..c4604a1d9108d99d66e4a3dd970f1519d8e0ebab 100644
--- a/expo-sqlite/session.d.cts
+++ b/expo-sqlite/session.d.cts
@@ -1,4 +1,4 @@
-import type { SQLiteDatabase, SQLiteRunResult, SQLiteStatement } from 'expo-sqlite';
+import type { SQLiteDatabase, SQLiteRunResult } from 'expo-sqlite';
 import { entityKind } from "../entity.cjs";
 import type { Logger } from "../logger.cjs";
 import type { RelationalSchemaConfig, TablesRelationalConfig } from "../relations.cjs";
@@ -32,13 +32,13 @@ export declare class ExpoSQLitePreparedQuery<T extends PreparedQueryConfig = Pre
     values: T['values'];
     execute: T['execute'];
 }> {
-    private stmt;
+    private client;
     private logger;
     private fields;
     private _isResponseInArrayMode;
     private customResultMapper?;
     static readonly [entityKind]: string;
-    constructor(stmt: SQLiteStatement, query: Query, logger: Logger, fields: SelectedFieldsOrdered | undefined, executeMethod: SQLiteExecuteMethod, _isResponseInArrayMode: boolean, customResultMapper?: ((rows: unknown[][]) => unknown) | undefined);
+    constructor(client: SQLiteDatabase, query: Query, logger: Logger, fields: SelectedFieldsOrdered | undefined, executeMethod: SQLiteExecuteMethod, _isResponseInArrayMode: boolean, customResultMapper?: ((rows: unknown[][]) => unknown) | undefined);
     run(placeholderValues?: Record<string, unknown>): SQLiteRunResult;
     all(placeholderValues?: Record<string, unknown>): T['all'];
     get(placeholderValues?: Record<string, unknown>): T['get'];
diff --git a/expo-sqlite/session.d.ts b/expo-sqlite/session.d.ts
index 830f45cfe3cf6704617714258111aada434ed16f..a8fda1838c001b1daeff0050ac2ca8de844db3bb 100644
--- a/expo-sqlite/session.d.ts
+++ b/expo-sqlite/session.d.ts
@@ -1,4 +1,4 @@
-import type { SQLiteDatabase, SQLiteRunResult, SQLiteStatement } from 'expo-sqlite';
+import type { SQLiteDatabase, SQLiteRunResult } from 'expo-sqlite';
 import { entityKind } from "../entity.js";
 import type { Logger } from "../logger.js";
 import type { RelationalSchemaConfig, TablesRelationalConfig } from "../relations.js";
@@ -32,13 +32,13 @@ export declare class ExpoSQLitePreparedQuery<T extends PreparedQueryConfig = Pre
     values: T['values'];
     execute: T['execute'];
 }> {
-    private stmt;
+    private client;
     private logger;
     private fields;
     private _isResponseInArrayMode;
     private customResultMapper?;
     static readonly [entityKind]: string;
-    constructor(stmt: SQLiteStatement, query: Query, logger: Logger, fields: SelectedFieldsOrdered | undefined, executeMethod: SQLiteExecuteMethod, _isResponseInArrayMode: boolean, customResultMapper?: ((rows: unknown[][]) => unknown) | undefined);
+    constructor(client: SQLiteDatabase, query: Query, logger: Logger, fields: SelectedFieldsOrdered | undefined, executeMethod: SQLiteExecuteMethod, _isResponseInArrayMode: boolean, customResultMapper?: ((rows: unknown[][]) => unknown) | undefined);
     run(placeholderValues?: Record<string, unknown>): SQLiteRunResult;
     all(placeholderValues?: Record<string, unknown>): T['all'];
     get(placeholderValues?: Record<string, unknown>): T['get'];
diff --git a/expo-sqlite/session.js b/expo-sqlite/session.js
index 23dcc2a7448137b64d7e54887ce5a003a413aff4..a3b3eb01687e5482236b374bd42565a90c6b25f3 100644
--- a/expo-sqlite/session.js
+++ b/expo-sqlite/session.js
@@ -7,6 +7,46 @@ import {
   SQLiteSession
 } from "../sqlite-core/session.js";
 import { mapResultRow } from "../utils.js";
+// ─── lark patch (Phase B N0b-2, criterion 17b) ──────────
+//
+// Upstream `prepareQuery()` calls `client.prepareSync()` and hands the native
+// statement to the prepared query, which never finalizes it. Measured on a real
+// device: 10,000 selects prepare 10,000 statements and release none
+// (drizzle-orm#4519 — open, no maintainer response, no linked PR).
+//
+// The fix keeps the CLIENT and the SQL instead of the statement, and prepares
+// inside each method. Two constraints shape it:
+//
+//   1. Expo's `executeSync()` returns a cursor that still has to be read, so
+//      the result is consumed BEFORE the statement is released. Releasing first
+//      passes every write and breaks every query.
+//   2. After a FAILED execute, `finalizeSync()` throws — and what it throws is
+//      the statement's own last error ("… has been rejected. → Caused by: Error
+//      code : UNIQUE constraint failed: …"). That is `sqlite3_finalize()` as
+//      documented: it reports the last evaluation's code and destroys the
+//      statement either way. Letting it out of the `finally` would replace the
+//      real error with a message about finalizeSync, so it is swallowed — but
+//      ONLY when an error is already on its way up.
+function larkWithStatement(client, sqlText, execute, consume) {
+  const stmt = client.prepareSync(sqlText);
+  let failed = false;
+  try {
+    return consume(execute(stmt));
+  } catch (err) {
+    failed = true;
+    throw err;
+  } finally {
+    if (failed) {
+      try {
+        stmt.finalizeSync();
+      } catch {
+        // Already reported by the error being propagated.
+      }
+    } else {
+      stmt.finalizeSync();
+    }
+  }
+}
 class ExpoSQLiteSession extends SQLiteSession {
   constructor(client, dialect, schema, options = {}) {
     super(dialect);
@@ -17,9 +57,8 @@ class ExpoSQLiteSession extends SQLiteSession {
   static [entityKind] = "ExpoSQLiteSession";
   logger;
   prepareQuery(query, fields, executeMethod, isResponseInArrayMode, customResultMapper) {
-    const stmt = this.client.prepareSync(query.sql);
     return new ExpoSQLitePreparedQuery(
-      stmt,
+      this.client,
       query,
       this.logger,
       fields,
@@ -58,9 +97,9 @@ class ExpoSQLiteTransaction extends SQLiteTransaction {
   }
 }
 class ExpoSQLitePreparedQuery extends SQLitePreparedQuery {
-  constructor(stmt, query, logger, fields, executeMethod, _isResponseInArrayMode, customResultMapper) {
+  constructor(client, query, logger, fields, executeMethod, _isResponseInArrayMode, customResultMapper) {
     super("sync", executeMethod, query);
-    this.stmt = stmt;
+    this.client = client;
     this.logger = logger;
     this.fields = fields;
     this._isResponseInArrayMode = _isResponseInArrayMode;
@@ -70,18 +109,24 @@ class ExpoSQLitePreparedQuery extends SQLitePreparedQuery {
   run(placeholderValues) {
     const params = fillPlaceholders(this.query.params, placeholderValues ?? {});
     this.logger.logQuery(this.query.sql, params);
-    const { changes, lastInsertRowId } = this.stmt.executeSync(params);
-    return {
-      changes,
-      lastInsertRowId
-    };
+    return larkWithStatement(
+      this.client,
+      this.query.sql,
+      (stmt) => stmt.executeSync(params),
+      (result) => ({ changes: result.changes, lastInsertRowId: result.lastInsertRowId })
+    );
   }
   all(placeholderValues) {
-    const { fields, joinsNotNullableMap, query, logger, stmt, customResultMapper } = this;
+    const { fields, joinsNotNullableMap, query, logger, client, customResultMapper } = this;
     if (!fields && !customResultMapper) {
       const params = fillPlaceholders(query.params, placeholderValues ?? {});
       logger.logQuery(query.sql, params);
-      return stmt.executeSync(params).getAllSync();
+      return larkWithStatement(
+        client,
+        query.sql,
+        (stmt) => stmt.executeSync(params),
+        (result) => result.getAllSync()
+      );
     }
     const rows = this.values(placeholderValues);
     if (customResultMapper) {
@@ -92,9 +137,14 @@ class ExpoSQLitePreparedQuery extends SQLitePreparedQuery {
   get(placeholderValues) {
     const params = fillPlaceholders(this.query.params, placeholderValues ?? {});
     this.logger.logQuery(this.query.sql, params);
-    const { fields, stmt, joinsNotNullableMap, customResultMapper } = this;
+    const { fields, client, joinsNotNullableMap, customResultMapper } = this;
     if (!fields && !customResultMapper) {
-      return stmt.executeSync(params).getFirstSync();
+      return larkWithStatement(
+        client,
+        this.query.sql,
+        (stmt) => stmt.executeSync(params),
+        (result) => result.getFirstSync()
+      );
     }
     const rows = this.values(placeholderValues);
     const row = rows[0];
@@ -109,7 +159,12 @@ class ExpoSQLitePreparedQuery extends SQLitePreparedQuery {
   values(placeholderValues) {
     const params = fillPlaceholders(this.query.params, placeholderValues ?? {});
     this.logger.logQuery(this.query.sql, params);
-    return this.stmt.executeForRawResultSync(params).getAllSync();
+    return larkWithStatement(
+      this.client,
+      this.query.sql,
+      (stmt) => stmt.executeForRawResultSync(params),
+      (result) => result.getAllSync()
+    );
   }
   /** @internal */
   isResponseInArrayMode() {
