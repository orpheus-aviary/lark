node_modules/

# Build output
dist/
dist-publish/
out/
build/
release/
*.tsbuildinfo
# The publishable CLI tarball (`just pack-cli`). The release gate verifies and
# publishes the same file; it is an artifact, not a source.
*.tgz

# Spike runtime state / generated fixtures
spikes/**/fixtures/
spikes/**/.runtime/

# SQLite
*.db
*.db-wal
*.db-shm

.env
.env.local
*.log
.DS_Store
coverage/
.turbo/
.vite/

# Vendored ffmpeg: reproduced by `just fetch-ffmpeg` from vendor/ffmpeg.lock.json
vendor/ffmpeg/
