// Side-effect-free version constant (M6-17). The protocol gate the CLI
// compares against daemons is `LOCAL_API_VERSION` in `@lark/shared` — this one
// is display only.

export const CLI_VERSION = '0.5.1';
