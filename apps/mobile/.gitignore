# Generated by `expo prebuild` (CNG). Everything that shapes the native
# project belongs in app.config.ts — if something can only be expressed by
# editing these, it belongs in a config plugin instead.
/android/
/ios/
/.expo/

# adb pulls, acceptance fixtures, measurement output.
/.runtime/
