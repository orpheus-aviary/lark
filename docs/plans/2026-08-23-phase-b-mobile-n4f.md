# Phase B · N4f 收藏夹 / 合集：展开、勾选、成批提交（`apps/mobile`）

- **日期**：2026-08-23（v1，**决策 a–h 待关闭**）
- **这是 N4 子计划里 N4f 那一行的展开**，不替代它：`docs/plans/2026-08-20-phase-b-mobile-n4.md` 的 **§2.5（添加页状态机，N4d + N4f）**· 判据 **31–33** · 决策 **i（提交语义）** 全部原样继承。
- **前置**：**N4e 已完成**（head `caa6c53`，判据 24 · 26 · 27 · 28 · 29 · 30①③ 真机全绿）。手机上已经有：添加页 · 任务列表 · 分享 intent · 引擎 + 前台服务 + 落盘协议 · **一个能用的模型**（批量的 LLM 门因此第一次验得了「配了就能过」——这正是 N4e 排在 N4f 之前的理由）。
- **基线**：`just check` exit 0 · `just test` exit 0 / **2875 passed**（shared 143 · core 1271 · mobile 138 · cli 428+9 skipped · daemon 468 · gui 427）· `mobile-typecheck` exit 0。
- **冻结设备**：vivo V2408A（Android 15 / API 35），行为判据一律 **release 构建**。
- 🔑 **本批按新的测试规模走**（N4e 子计划 §8.5，用户 2026-08-23 定）：**默认落单测**，只有设备能回答的才上机；**反测全部在 vitest 里做**；**整批只上机一次**。§4 每条判据都标了归属。

---

## §0 范围

**做**：`fetchList` 的移动薄壳（展开 + 部分成功文案）· 条目选择模型（纯函数：全选 / 反选 / 计数 / 上限）· 目标选择（**仅曲库 / 已有歌单 / 新建歌单（默认用列表标题）**）· `ui/list-picker.tsx` 全屏勾选页 · 批次提交（一个列表组 = 一次 `enqueueBatches`）· 任务列表里的**批次进度 M/N** · 判据 **31–33**。

**不做（本批）**：ensure-file 与缓存管理（N4g）· 歌单导出（N4g）· **歌曲行的多选批量操作**（N6——本批的「多选」只在列表条目里）· 批次级取消（判据没有要，`engine.cancel` 仍是逐任务）· 失败条目的重试入口（N4d 决策 i 留下的，仍不做）· 同步（N5，**TLS 仍硬阻塞**）。

**一句话的边界**：N4e 之后手机能下「一首歌」；N4f 之后它能下「一整个收藏夹」，而且**在下之前你先看见里面有什么、能划掉不要的**。

---

## §1 开工前必须知道的

### 1.1 portable 那半已经全部写好了，本批基本是接线

| 要的东西 | 已经在哪 | 形状 |
|---|---|---|
| 认出收藏夹 / 合集 | `portable/download/link.ts:96-99`（`parseSongInput` 里） | `{ kind:'favorites', media_id, url }` / `{ kind:'collection', mid, season_id, url }` |
| 展开成条目 | `portable/download/preflight.ts:138` `fetchList` | `{ title, videos: {bvid,title,duration}[], error }` |
| 批量的 LLM 门 | `preflight.ts:113` `preflightBatch` | **零网络**；有 keyword 条目或 `clean` 条目而没模型 → 抛 |
| 成批入队 | `portable/download/engine.ts:313` `enqueueBatches` | 吃 `DownloadBatchGroupInput[]`，回 `DownloadBatchData[]` |
| 批次状态 | hub 已经带着（`downloads/hub.ts:35`，`onBatchesChanged` 从 N4b 就接上了） | `{ id, target, total, items[], created_at }` |

**手机上缺的只有三样**：展开的调用点、勾选的 UI、目标是「新建歌单」时的那条路。`downloads/preflight.ts:74` 的 `LIST_NOT_YET`（「收藏夹和合集要等下一批」）就是本批要拆掉的那句话。

### 1.2 🔴 N4e 刚踩过的坑，这一批的形状完全一样

`recognise` 对收藏夹/合集今天返回 `{ kind:'refused', message: LIST_NOT_YET }`——**和 N4e 之前的 keyword 分支一模一样**：一句写死的、说「等下一批」的话。N4e 的教训是「门开了之后没人接，而没有任何东西在看那条路」，代价是一次真机会话。**本批一开始就要有「展开成功」那条路的单测**，不是等设备告诉我。

### 1.3 部分成功是常态，而且它已经把截断折进 `error` 了

`fetchList` 从不为「拉到一半失败」抛错：拿到过东西就返回，`error` 说为什么停（`preflight.ts:178-186`）。**截断也走同一个字段**——`error === null && truncated` 时它自己填一句「列表过长，只取回了前 N 条（上限 200 页 / 5000 条）」。所以 UI 只需要**照实显示 `error`**，判据 32 的反测（去掉 `truncated → error`）也就落在 core 的单测里而不是设备上。

**只有一种情况会抛**：一条都没拿到（`videos.length === 0`）。那是「这个链接用不了」，不是部分成功。

### 1.4 「准入原子 / 执行逐项」是两件事，UI 必须让人分得出来

`enqueueBatches` 的原子性是**准入**的（`engine.ts:313-330`：校验目标 → 查重 → 容量 → 建歌单 → 注册）。**它不承诺所有下载都会成功**。落到屏幕上是两种完全不同的反馈：

- **准入失败** → 一句话，**当场**，在提交按钮下面（歌单没建、一条任务都没进队）。
- **执行失败** → 任务列表里那一条红字，批次进度照走（`total` 不变，失败的条目计进已完成）。

判据 33 要的就是这两者在 UI 上区分得出来。**不要用同一个提示区显示两者**——那正是 T5 记过的「两个数字渲染进一个字符串」。

### 1.5 5000 条上限落在手机上是什么样

`FETCH_LIST_ITEMS_MAX = 5000` / `DOWNLOAD_BATCH_ITEMS_MAX = 1000`（`shared/limits.ts`）。也就是说**一次展开最多 5000 行，而一次批量最多提交 1000 条**——两个数不一样，勾选超过 1000 就必须在提交前说清楚，不能等 `enqueueBatches` 抛。

渲染上：`FlatList` + **勾选状态不进每行**（一个 `Set<bvid>` 在页面顶层），否则 5000 个 `useState` 会把这屏卡死。N4d 已经记过「`FlatList` 只渲染可见行」——这次它是优点，但也意味着「全选」不能靠遍历已渲染的行。

### 1.6 收藏夹进不来分享面板，只能粘贴

N0b-4c 实测：bilibili 的收藏夹分享直接进它自己的发动态发布器，到不了系统 chooser。**所以本批的唯一入口是粘贴框**，分享 intent 那条路不用管（它送过来的永远是单个视频）。

### 1.7 展开是一次可能很慢的网络往返

200 页 × 每页一次请求，最坏几十秒。三件事因此要定：**要有「正在取列表」态**（N4d 的短链已经有这个形状）· **要能取消**（`AbortController`，随离开页面而 abort）· **展开不属于「下载」，不 `arm()` 前台服务**——它在前台、有人在看着屏幕，而 `arm()` 是为熄屏后的下载准备的。

---

## §2 目标结构

### 2.1 文件布局

```
apps/mobile/src/
├── downloads/
│   ├── preflight.ts        ← 改（N4f-1）：Recognition 加 list 分支，删 LIST_NOT_YET；
│   │                       #   expandList() 薄壳 + submitBatch() 提交映射
│   └── selection.ts        ← 新（N4f-1）：勾选模型（纯函数，进 vitest）
└── ui/
    ├── list-picker.tsx     ← 新（N4f-2）：全屏勾选页（FlatList + 全选/反选 + 目标 + 提交）
    ├── add-tab.tsx         ← 改（N4f-2）：list 分支进入选择页
    └── task-list.tsx       ← 改（N4f-2）：批次进度 M/N 一行
```

### 2.2 状态机（N4 §2.5 的 favorites/collection 那一支，展开）

```
粘贴收藏夹/合集链接
      ↓  （离线 parse，零网络）
  [列表已识别] ──→ 点「展开」──→ [正在取列表…]（可取消）
                                    ↓
                    ┌───────────────┴───────────────┐
                    ↓                               ↓
              一条都没拿到                    拿到 N 条（error 可能非空）
              → 拒绝，说原因                        ↓
                                            全屏勾选页
                                            · 标题 = 列表标题
                                            · error 非空 → 顶部一行照实说
                                            · 全选 / 反选 / 计数
                                            · 命名模式（记忆，无模型时 clean 灰）
                                            · 目标：仅曲库 / 已有歌单 / 新建（默认列表标题）
                                                    ↓
                                            提交 = 一次 enqueueBatches
                                                    ↓
                                      准入失败 → 当场一句话（歌单没建）
                                      准入成功 → 回添加页，任务列表出现批次进度 M/N
```

### 2.3 提交映射（照桌面，决策 i 原样继承）

```ts
// 一个列表组 = 一个 group = 一次 enqueueBatches
enqueueBatches([{
  target,                                  // BatchTargetInput
  items: chosen.map((v) => ({
    kind: 'video', bvid: v.bvid, page: null,
    title: v.title,                        // 列表给的标题，clean 的回落值
    naming: mode,                          // 整组一个模式（决策 c）
  })),
}])
```

`target` 三选一：`{kind:'all'}`（仅曲库）· `{kind:'playlist', playlist_id}` · `{kind:'new', name}`。**「新建歌单」的默认名就是 `fetchList` 回的 `title`**（判据 31 点名要这个）。

---

## §3 批次划分

| 批 | 内容 | 需要设备 | 判据 |
|---|---|---|---|
| **N4f-1** | **逻辑与接线**：`selection.ts`（勾选模型 + 上限）· `preflight.ts` 的 list 分支 + `expandList` + `submitBatch` · **单测**（含判据 32 的反测、判据 33 的准入/执行分离、「门开了那条路」） | 否 | 32 · 33 的逻辑半边 |
| **N4f-2** | **UI + 一次上机**：`list-picker.tsx` · 添加页接入 · 任务列表的批次进度 · **一次真机会话** | 是 | **31**，32 · 33 的设备半边 |

**顺序理由**：勾选模型和提交映射是纯逻辑且是判据 32/33 的落点，先写完它们，UI 才只剩渲染；**整批只上机一次**（新规矩），所以设备那一次要等 UI 完整。

---

## §4 判据（31–33，**每条标归属**）

31. **【上机】** 粘一条**收藏夹**链接 → 展开成条目列表 → 全选/反选 → 目标选「新建歌单（用列表标题）」→ 提交 → 批次进度 M/N 走到底，歌单里是勾选的那些。**合集同走一遍**。顺带看展开 200+ 条时的掉帧。
32. **部分成功如实说**。**【单测】** `fetchList` 截断时 `error` 非空且点名上限（**反测**：去掉 `truncated → error` 那一步 → 必须红）· 薄壳把 `error` 原样递给 UI，不改写 · 一条都没拿到时是拒绝而不是空列表。**【上机】** 只看一眼：`error` 非空时那一行真的在屏幕上。
33. **准入与执行分开**。**【单测】** 准入失败（空歌单名 / 超 `DOWNLOAD_BATCH_ITEMS_MAX`）→ **没建歌单、没入队**，错误从 `enqueueBatches` 出来；执行失败不影响同批其它条目。**【上机】** 两种反馈落在**不同的位置**（准入 = 提交按钮下面那行；执行 = 任务列表里那条红字）。

**不验**：批次级取消 · 5000 条满载 · 合集的分页边界（`FETCH_LIST_PAGES_MAX`）——都记成「有代码、有上限常量、没跑过」。

---

## §5 决策（a–h，**待关闭**，每条给了倾向）

| # | 决策 | 倾向 | 理由 |
|---|---|---|---|
| **a** | 勾选页是全屏页还是 sheet | **全屏页**（`Modal` 覆盖，带自己的返回） | 200 条起步的列表，sheet 的 2/3 屏高（`shell.tsx` 的 `sheetHolder`）放不下；而且它有自己的目标选择与提交，是一个「屏幕」不是一个「选项」 |
| **b** | 展开是自动还是要点一下 | **要点一下**（识别后显示「N 条？展开」按钮） | 展开是一次几十秒的网络往返（§1.7），而粘贴框是**每次防抖都会重跑识别**的——自动展开等于每敲一下就打一次 bilibili |
| **c** | 命名模式：整组一个，还是逐条 | **整组一个**，用记忆的模式（`readNamingMode`），无模型时 `clean` 灰 | 桌面就是整组一个（`BatchSelectModal`）；逐条在手机上要给 200 行各加一个 chip，而没人会逐条选 |
| **d** | 勾选超过 `DOWNLOAD_BATCH_ITEMS_MAX`（1000） | **提交按钮当场禁用并说「最多 1000 条，已选 N」** | 让 `enqueueBatches` 抛也能工作，但那是「点了才知道」；这个数在提交前就完全可知 |
| **e** | 默认全选还是全不选 | **默认全选** | 粘一个收藏夹进来，意图默认是「都要」；判据 31 的「反选」正好从这里出发 |
| **f** | 目标默认值 | **「新建歌单」+ 列表标题**，可改成仅曲库 / 已有歌单 | 与 e 同一个意图：一个收藏夹进来最可能想成为一个歌单。判据 31 点名了这个选项 |
| **g** | 展开失败（一条没拿到）怎么说 | **拒绝态，用 `fetchList` 抛出来的原话** | 与 N4d/N4e 一致：拒绝语是 portable 的，手机不改写（唯一自己写的那句 `LIST_NOT_YET` 本批删掉） |
| **h** | 批次进度显示在哪 | **任务列表顶部一行**（`M/N · 目标名`），不进每条任务 | hub 已经带着 `batches`；每条任务重复批次信息是噪音，而「这一批走到哪了」是一个数 |

---

## §6 风险

| 风险 | 缓解 |
|---|---|
| **展开把 UI 卡住**（200 页往返） | 「正在取列表」态 + `AbortController`（离开即 abort）；**不 `arm()` 前台服务**（§1.7） |
| **5000 行列表掉帧** | `FlatList` + 勾选状态是页面顶层的一个 `Set`（§1.5）；判据 31 顺带看掉帧，但**不为它定数值判据**（没有基线） |
| 🔴 **重复 N4e 那个「门开了没人接」** | §1.2：**N4f-1 就要有「展开成功」那条路的单测**，不靠设备发现 |
| **准入与执行被 UI 揉成一句话** | §1.4：两个反馈两个位置，判据 33 的设备半边只看这一件事 |
| **真收藏夹的内容不可控**（条目会变、可能有失效视频） | 这是优点：判据 33 的「执行逐项成败」正需要一条会失败的。**但判据 31 的「歌单里是勾选的那些」要按 bvid 比对，不按条数** |
| **展开一个大收藏夹会真下几百首歌** | 判据 31 用**勾选少数几条**跑通全流程；「全选」只验证计数与按钮状态，不真提交 5000 条 |

---

## §7 本批不会证明的事（先写在这里）

- **批次级取消**（判据没有要；`engine.cancel` 仍是逐任务，「全部取消」是 N4d 那个全局按钮）。
- **满载**：5000 条展开、1000 条批量提交，都只有上限常量和单测，没有真机证据。
- **合集的分页边界**（`FETCH_LIST_PAGES_MAX = 200`）。
- **失败条目的重试**（N4d 决策 i 留下的，仍要手动重新粘）。
- **歌曲行的多选批量操作**（N6）——本批的「多选」只存在于列表条目里。
- **同步**：新建的歌单会进 `sync_changes`，但 N5 之前没有东西会把它推出去；**TLS 仍硬阻塞 N5**。
